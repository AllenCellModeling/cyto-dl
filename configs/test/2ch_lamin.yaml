# @package _global_

# to execute this experiment run:
# python train.py experiment=example

defaults:
  - override /datamodule: patch_multitask.yaml
  - override /model: 2ch_lamin.yaml
  - override /trainer: gpu.yaml
  - override /logger: mlflow_im2im.yaml

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

tags: ["dev"]
seed: 12345

model:
  patch_shape: [32, 256, 256]
  save_dir: //allen/aics/assay-dev/users/Benji/CurrentProjects/20xBF_to_cellseg/laminseg_separate_interphase_and_mitotic_channel/3d_seg_2d_mito_mask

ckpt_path: "//allen/aics/assay-dev/users/Benji/CurrentProjects/20xBF_to_cellseg/laminseg_separate_interphase_and_mitotic_channel/3d_seg_2d_mito_mask/epoch_049.ckpt"
datamodule:
  path: //allen/aics/assay-dev/users/Benji/CurrentProjects/20xBF_to_cellseg/laminseg_separate_interphase_and_mitotic_channel/3d_seg_2d_mito_mask/manifest_with_mitotic.csv
  batch_size: 1
logger:
  mlflow_im2im:
    experiment_name: benji_2ch_lamin
    run_name: mixed_3d_2d_debug_overfit

sliding_window_fns:
  process_fn:
    _target_: src.utils.expand_2d_to_3d
    _partial_: True
  postprocess_fn:
    _target_: src.utils.extract_best_z
    _partial_: True