# @package _global_

# to execute this experiment run:
# python train.py experiment=example

defaults:
  - override /datamodule: intensity_transfer.yaml
  - override /model: intensity_transfer_gan.yaml
  - override /callbacks: default.yaml
  - override /trainer: gpu.yaml
  - override /logger: mlflow.yaml

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

tags: ["dev"]
seed: 12345
source_col: YOUR_SOURCE_NAME
target_col: YOUR_TARGET_NAME

experiment_name: YOUR_EXP_NAME
run_name: YOUR_RUN_NAME

trainer:
  max_epochs: 100
  precision: 32 # THIS IS CURRENTLY NECESSARY FOR GANS

model:
  patch_shape: [12, 256, 256]

datamodule:
  path: ${paths.data_dir}/YOUR_DATA_PATH/
  cache_dir: ${paths.data_dir}/YOUR_DATA_PATH/cache
  subsample:
    train: -1
    test: 1
    valid: -1
