# @package _global_
# to execute this experiment run:
# python train.py experiment=example
defaults:
  - /model: im2im/omnipose.yaml
  - /callbacks: default.yaml
  - /trainer: gpu.yaml
  - /logger: mlflow.yaml
  - /data: im2im/patch_segmentation.yaml
  - /paths: default.yaml
  - /extras: default.yaml
  - /hydra: default.yaml
# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

tags: ["dev"]
seed: 12345
source_col: raw # YOUR_RAW_COLUMN_NAME
target_col: seg # YOUR_SEGMENTATION_COLUMN_NAME
experiment_name: YOUR_EXP_NAME
run_name: YOUR_RUN_NAME
task_name: train

trainer:
  max_epochs: 1000

model:
  patch_shape: [32, 256, 256]

data:
  path: /allen/aics/assay-dev/users/Benji/CurrentProjects/im2im_dev/aics-im2im/data/gt_to_im #${paths.data_dir}/DATAPATH/
  cache_dir: ${paths.data_dir}/DATAPATH/cache
  subsample:
    train: 1
    test: 1
    valid: 1
  batch_size: 2
