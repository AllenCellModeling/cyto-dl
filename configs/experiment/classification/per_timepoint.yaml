# @package _global_

# to execute this experiment run:
# python train.py experiment=example

defaults:
  - override /data: classification/single_timepoint.yaml
  - override /model: classification/single_timepoint.yaml
  - override /callbacks: default.yaml
  - override /trainer: gpu.yaml
  - override /logger: mlflow.yaml

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

tags: ["dev"]
seed: 12345

experiment_name: breakdown_classification
run_name: single_timepoint
persist_cache: False
ckpt_path: "//allen/aics/assay-dev/users/Benji/CurrentProjects/im2im_dev/cyto-dl/logs/train/runs/breakdown_classification/single_timepoint/2024-02-05_13-00-47/checkpoints/epoch_109.ckpt"

trainer:
  max_epochs: 130
  check_val_every_n_epoch: 10

data:
  path: //allen/aics/assay-dev/users/Benji/hydra_workflow/all_nucmorph/5/formation_breakdown
  cache_dir: /storage/benji.the.kid/cache/formation_breakdown
