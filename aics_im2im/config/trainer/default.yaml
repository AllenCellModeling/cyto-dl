defaults:
  - /defaults/trainer/default@_here_ #inherit serotiny's trainer config
  - callbacks: default

_target_: pytorch_lightning.Trainer
max_epochs:
gpus:
enable_checkpointing: True
precision: 16

reload_dataloaders_every_n_epochs: 0
check_val_every_n_epoch: 10
log_every_n_steps: 1